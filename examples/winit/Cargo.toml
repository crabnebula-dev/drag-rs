[package]
name = "winit-app"
version = "0.0.0"
publish = false
edition = "2021"

[target."cfg(not(target_os = \"linux\"))".dependencies]
drag.workspace = true
winit = { version = "0.30", default-features = false, features = [ "rwh_05" ] }
